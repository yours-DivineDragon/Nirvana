# Integer halving example

Given a JSON object containing a non-negative integer `value`, return a JSON object containing `result`, equal to half of `value`.

This example deliberately omits the odd-number rounding rule so the harness can surface a specification ambiguity.
